import { CompletionOverTime } from "@/components/charts/completion-over-time"

export const AdminDashboard = () => {
    return (
        <div>
            Hello From Admin
            <CompletionOverTime />
        </div>
    )
}
